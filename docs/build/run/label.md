# Run.label

The name given to this run with `-n`. AGL names the run's branch after it: `agl/<label>`. A label is unique in a repository. To reuse one,
[clear the old run](../../run-workflows/index.md#agl-clear) first.

## Reference

::: agl.sdk.Run.label
