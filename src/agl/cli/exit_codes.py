"""Re-exports EXIT_CODES and exit_code_for from ports.errors, and holds no table of its own.

The one exception-to-exit-code table is in ports/errors.py, beside the classes it maps. What
to do with an exception that is not an AglError is this module's only decision.
"""
