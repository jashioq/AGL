Read this repository, then turn the issues below into fixes that can be made side by side: each
fix changes files that no other fix changes. Change nothing yourself.

{{str}}

Call `record_triage` once, at the end, with every fix.
