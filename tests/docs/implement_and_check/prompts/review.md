Review the last commit in this repository against what it was asked to do, below. Change
nothing.

{{str}}

This is what the project's build command did with the work, as `passed`, `status` and `output`:

{{VerifierOutcome}}

A failing build is the first finding. Report only what a maintainer would ask to have changed
before merging. Call `record_review` once, at the end, with every finding, or with none.
