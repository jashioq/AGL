[project]
name = "implement_and_check"
version = "0.1.0"

[project.entry-points."agl.workflows"]
implement_and_check = "implement_and_check:implement_and_check"

[tool.agl]
requires = "agents-gl>=0.0.8"
config = ["build"]
